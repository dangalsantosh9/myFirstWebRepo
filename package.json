{
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "compile": "npm run clean && tsc --watch",
    "clean": "rimraf dist",
    "start-dev": "node --es-module-specifier-resolution=node --no-warnings --watch dist/index.js",
    "start-once": "node --es-module-specifier-resolution=node --no-warnings dist/index.js",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.9",
    "rimraf": "^6.1.2",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "zod": "^4.3.5"
  }
}
